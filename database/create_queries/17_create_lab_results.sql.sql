CREATE TABLE lab_results (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  patient_id integer,
  test_id integer,
  visit_instance_id integer,
  result_value decimal,
  result_date date,
  result_status varchar,
  critical_result_flag boolean,
  reference_low decimal,
  reference_high decimal
);

--
ALTER TABLE lab_results ADD FOREIGN KEY (patient_id) REFERENCES patients (id);

ALTER TABLE lab_results ADD FOREIGN KEY (test_id) REFERENCES laboratory_tests (id);

ALTER TABLE lab_results ADD FOREIGN KEY (visit_instance_id) REFERENCES patient_visits (id);